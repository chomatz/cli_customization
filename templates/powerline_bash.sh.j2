if [ "$UID" -ne 0 ]; then
	if [ -f $(which powerline-daemon) ]; then
		powerline-daemon -q
		POWERLINE_BASH_CONTINUATION=1
		POWERLINE_BASH_SELECT=1
		. {{ bash_powerline.stdout }}
	fi
fi
alias grep='grep --color=auto'
alias ll='ls -l --color=auto'
alias ls='ls --color=auto'
